:plain
  if($('#modal_form').length == 0) {
    $("body").append("#{j(render('shared/modal_form'))}");
    $("#modal_form").modal('show');
  } else {
    $("#modal_form").html("#{j(render(@form_name || 'modal_form_content'))}");
  }
