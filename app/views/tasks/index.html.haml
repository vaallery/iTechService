.page-header
  %h1=t '.title', :default => Task.model_name.human.pluralize
  = link_to t('.new', :default => t("helpers.links.new")), new_task_path, :class => 'btn btn-primary'
  
#resource_table= render 'table'