<%= form_tag service_jobs_inventory_path, method: :get, class: 'form-inline' do %>
  <%= select_tag :department_id, options_from_collection_for_select(Department.all, :id, :name, @department.id) %>
  <%= submit_tag 'Найти', class: 'btn btn-primary' %>
<% end %>

<%= render 'list', service_jobs: @service_jobs %>
