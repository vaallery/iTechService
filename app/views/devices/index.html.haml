.page-header
  %h1=t '.title', :default => Device.model_name.human.pluralize
  = link_to t('.new', :default => t("helpers.links.new")), new_device_path, :class => 'btn btn-primary'
  
.table_filter.navbar.container
  .navbar-inner
    %ul.nav
      %li.dropdown
        = link_to '#', class: 'dropdown-toggle', 'data-toggle' => 'dropdown' do
          = t('status')
          %b.caret
        %ul.dropdown-menu
          %li= link_to t('all'), devices_path
          %li= link_to t('done'), devices_path(status: 'done')
          %li= link_to t('undone'), devices_path(status: 'undone')
          %li= link_to t('repair'), devices_path(status: 'repair')
    
    = form_tag devices_path, method: 'get', id: 'search_devices_form', class: 'navbar-search form-search', remote: true do
      .input-append
        = text_field_tag :ticket, params[:ticket], id: 'search_ticket_input', class: 'search-query input-medium',
            placeholder: 'ticket number'
        %span.add-on.btn.clear_search_input
          %i.icon-remove
      
      .input-append
        = text_field_tag :device, params[:device], id: 'search_device_input', class: 'search-query input-medium',
            placeholder: 'device serial number'
        %span.add-on.btn.clear_search_input
          %i.icon-remove
    
      .input-append
        = text_field_tag :client, params[:client], id: 'search_client_input', class: 'search-query input-medium',
            placeholder: 'client (name or phone)'
        %span.add-on.btn.clear_search_input
          %i.icon-remove
  
#resource_table= render 'table'
