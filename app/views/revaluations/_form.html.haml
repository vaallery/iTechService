= simple_form_for(@revaluation) do |f|
  = f.error_notification

  .form-inputs
    = f.association :revaluation_act
    = f.association :product
    = f.input :price

  .form-actions
    = f.button :submit
