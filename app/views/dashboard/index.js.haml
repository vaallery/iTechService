:plain
  $('#dashboard_table_filter').html("#{j(render(partial: @table_name+'_filter'))}");
  $('#dashboard_content').html("#{j(render(partial: @table_name))}");
  if(#{@location_name.present?})
    $('.devices_location').text(". #{@location_name}")
  else
    $('.devices_location').text("");
