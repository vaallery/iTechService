:plain
  $('#dashboard_content').html("#{j(render(partial: 'ready_devices_table'))}");
  $('.devices_location').text("");
  if($('#ready_devices_table_filter').length == 0)
    $('#dashboard_table_filter').html("#{j(render(partial: 'ready_devices_table_filter'))}");
