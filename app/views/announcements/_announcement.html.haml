= div_for announcement, class: announcement.kind do
  - if announcement.closeable_by? current_user
    = link_to announcement_path(announcement, announcement: {active: false}), method: :put, remote: true,
        class: 'close close_announcement_button' do
      &times;
  %span.announcement_content= announcement_content announcement