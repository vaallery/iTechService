= link_to (can?(:manage, karma) ? edit_karma_path(karma) : '#'), remote: true, id: "user_karma_#{karma.id}",
    class: "user_karma_link #{karma.good ? 'good' : 'bad'}", data: {comment: karma_comment(karma)} do
  %i{class: "icon-thumbs-#{karma.good ? 'up' : 'down'}"}
