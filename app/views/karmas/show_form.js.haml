- if @karma.new_record?
  == $target = $("#new_#{@karma.good ? 'good' : 'bad'}_karma_for_#{@karma.user_id}");
- else
  == $target = $("#user_karma_#{@karma.id}");
:plain
  close_karma_popovers();
  $target.popover({
    html: true,
    placement: 'top',
    title: "#{j(button_to_close_popover)}",
    content: "#{j(render('form'))}",
    container: '#karma_popover_container'
  });
  $target.popover('show');
  $('#karma_comment').focus();