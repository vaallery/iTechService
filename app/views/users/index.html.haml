.page-header
  %h1=t '.title', :default => User.model_name.human.pluralize
  = link_to t('.new', :default => t("helpers.links.new")), new_user_path, :class => 'btn btn-primary'

#resource_table= render 'table'