.page-header
  %h1=t '.title', :default => Info.model_name.human.pluralize
  = link_to t('.new', :default => t("helpers.links.new")), new_info_path, :class => 'btn btn-primary'

- if can? :manage, Info
  #resource_table= render 'table'
- elsif can? :read, Info
  = render 'table_read'