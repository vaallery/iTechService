%h1 Listing feature_types

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @feature_types.each do |feature_type|
    %tr
      %td= feature_type.name
      %td= link_to 'Show', feature_type
      %td= link_to 'Edit', edit_feature_type_path(feature_type)
      %td= link_to 'Destroy', feature_type, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Feature type', new_feature_type_path
