defaults: &defaults
  server: https://<%= ENV['SERVER_HOST'] %>:9292/faye
  secret_token: <%= ENV['PRIVATE_PUB_SECRET_TOKEN'] %>

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
  signature_expiration: 3600 # one hour

sandbox:
  <<: *defaults
  signature_expiration: 3600 # one hour
